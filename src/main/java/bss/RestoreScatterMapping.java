package bss;

import ij.IJ;
import ij.ImageJ;
import ij.ImagePlus;
import ij.plugin.PlugIn;

public class RestoreScatterMapping implements PlugIn
{

	@Override
	public void run( String arg )
	{
		ImagePlus imp = IJ.getImage();		
		if (imp == null)
		{
		    IJ.noImage();
		    return;
		}		
		FGParameters cfgParams = new FGParameters();
		if(!cfgParams.loadFromImagePlus( imp ))
		{
			//IJ.error( "This is not a cytofluorogram generated by LogColocalization plugin!" );
			return;
		}
		
		FGParameters.applyHistParameters(imp, cfgParams);
		IJ.log( "BigScopeScatter v." + BSSsettings.sVersion + ". Loaded image cytofluorogram parameters." );
		cfgParams.printParams();
	}

}
